name: Go

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go 1.x
        uses: actions/setup-go@v6
        with:
          go-version: ^1.25
        id: go

      - name: Check out code into the Go module directory
        uses: actions/checkout@v6

      - name: Build
        run: go build -v -ldflags "-s -w -X helmsec/version.Version=$GITHUB_REF -X helmsec/version.GitCommit=${GITHUB_SHA::8}" .
